% Auto-generated by cameraCalibrator app on 13-Nov-2014
%-------------------------------------------------------


% Define images to process
imageFileNames = {'../images/calib_example/Image1.tif',...
    '../images/calib_example/Image2.tif',...
    '../images/calib_example/Image3.tif',...
    '../images/calib_example/Image4.tif',...
    '../images/calib_example/Image5.tif',...
    '../images/calib_example/Image6.tif',...
    '../images/calib_example/Image7.tif',...
    '../images/calib_example/Image8.tif',...
    '../images/calib_example/Image9.tif',...
    '../images/calib_example/Image10.tif',...
    '../images/calib_example/Image11.tif',...
    '../images/calib_example/Image12.tif',...
    '../images/calib_example/Image13.tif',...
    '../images/calib_example/Image14.tif',...
    '../images/calib_example/Image15.tif',...
    };

% Detect checkerboards in images
[imagePoints, boardSize, imagesUsed] = detectCheckerboardPoints(imageFileNames);
imageFileNames = imageFileNames(imagesUsed);

% Generate world coordinates of the corners of the squares
squareSize = 25;  % in units of 'mm'
worldPoints = generateCheckerboardPoints(boardSize, squareSize);

% Calibrate the camera
[cameraParams, imagesUsed, estimationErrors] = estimateCameraParameters(imagePoints, worldPoints, ...
    'EstimateSkew', false, 'EstimateTangentialDistortion', false, ...
    'NumRadialDistortionCoefficients', 2, 'WorldUnits', 'mm');

% View reprojection errors
h1=figure; showReprojectionErrors(cameraParams, 'BarGraph');

% Visualize pattern locations
h2=figure; showExtrinsics(cameraParams, 'CameraCentric');

% Display parameter estimation errors
displayErrors(estimationErrors, cameraParams);

% For example, you can use the calibration data to remove effects of lens distortion.
% originalImage = imread(imageFileNames{1});
% undistortedImage = undistortImage(originalImage, cameraParams);

% See additional examples of how to use the calibration data.  At the prompt type:
% showdemo('MeasuringPlanarObjectsExample')
% showdemo('SparseReconstructionExample')

